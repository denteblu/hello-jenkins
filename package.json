{
  "name": "hello-jenkins",
  "description": "hello jenkins test app",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "3.12.0"
  },
  "devDependencies": {
    "mocha": "1.20.1",
    "mocha-jenkins-reporter": "0.3.8",
    "supertest": "0.13.0"
  },
  "scripts": {
    "test-jenkins": "JUNIT_REPORT_PATH=build/reports/report.xml mocha --colors --reporter mocha-jenkins-reporter"
  }
}
